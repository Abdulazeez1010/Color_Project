
import { styled } from "@mui/material/styles";

export const PaletteFooterRoot = styled("div")({
    backgroundColor: "white",
    height: "5vh",
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
    fontWeight: "bold"
});

export const PaletteEmoji = styled("span")({
    fontSize: "1.5rem",
    margin: "0 1rem"
});